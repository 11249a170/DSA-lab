Tower of Hanoi
Aim:
To understand and implement the Tower of Hanoi problem using recursion and analyze its time complexity.

Algorithm:
1. When the problem is reduced to a single disk, move that disk directly from Source to 
Destination, and return.  Base Condition. 
2. Move Top (n−1) Disks from Source to Auxiliary / Temporary rod using Des na on as temporary 
storage. 
3. Move the remaining disk from Source to Destination (Only one disk movement) 
4. Move (n−1) Disks from Auxiliary / Temporary to Des na on rod using Source as temporary 
storage 
5. Repeat Recursively until base condition satisfied. 

Program:
#include <stdio.h>

void hanoi(int n, char s, char a, char d)
{
    if(n == 1)
    {
        printf("\n move %d from %c to %c", n, s, d);
        return;
    }

    hanoi(n-1, s, d, a);
    printf("\n move %d from %c to %c", n, s, d);
    hanoi(n-1, a, s, d);
}

int main()
{
    int n;

    printf("enter the no of disks : ");
    scanf("%d", &n);

    hanoi(n, 'S', 'A', 'D');
}

Output:
enter the no of disks : 4

 move 1 from S to A
 move 2 from S to D
 move 1 from A to D
 move 3 from S to A
 move 1 from D to S
 move 2 from D to A
 move 1 from S to A
 move 4 from S to D
 move 1 from A to D
 move 2 from A to S
 move 1 from D to S
 move 3 from A to D
 move 1 from S to A
 move 2 from S to D
 move 1 from A to D

Result: Successfully implemented Tower of Hanoi using recursion and verified correct movement of disks.
